server {
    listen       80;
    server_name  localhost; 
    
    # 静态文件服务配置
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}